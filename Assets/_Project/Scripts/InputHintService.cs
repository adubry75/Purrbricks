// Stub — full implementation in Task 3
public enum HintKey
{
    LaunchBall,
    FuryStrikeTutorial,
    FuryStrikeBar,
    Radial,
    LevelCode,
    PauseInstruction
}

public static class InputHintService
{
    public static void SetScheme(InputScheme scheme) { }
    public static string Get(HintKey key) => string.Empty;
}
